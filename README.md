# How to run  

1. Install Dependencies First, you need to download all the packages listed in package.json:

```bash
pnpm install
```
2. Start the Development Server To run the app locally with hot-reloading (so you can see changes instantly):

```bash
pnpm dev
```

Once running, you can view the application by opening http://localhost:3000 in your web browser.